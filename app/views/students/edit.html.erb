<!DOCTYPE html>
<html lang="en">
<head>
  <title>Edit Student</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.0/jquery.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
</head>
<h1>Editing <%= @edit_student.first_name%><%= @edit_student.last_name%></h1>
<br>
    <% if flash[:error] %>
	    <p><%= flash[:error] %></p>
    <% end %>
    <form action="/dojos/<%= @edit_dojo.id%>/students/<%= @edit_student.id%>" method="post" style="width:300px; text-align:right;">
    <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
    <input type="hidden" name="_method" value="patch">
        First Name: <input type="text" name="student[first_name]" value="<%= @edit_student.first_name%>"><br><br>
        Last Name: <input type="text" name="student[last_name]" value="<%= @edit_student.last_name%>"><br><br>
        City: <input type="text" name="student[email]" value="<%= @edit_student.email%>"><br><br>
        Dojo: <select name="student[dojo_id]">
           <option value="<%= @edit_dojo.id%>"><%= @edit_dojo.branch%></option>
        </select><br><br>
        <input type="submit" Value="Update">
    </form>
    <br><br>
    <a href="/">Home</a> <a href="/dojos/<%= @edit_student.id %>">Show</a>
</html>